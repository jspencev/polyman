import add from './add';
import init from './init';
import local from './local';
import remove from './remove';

const commands = {
  add,
  init,
  local,
  remove
};

export {
  add,
  init,
  local,
  remove
};

export default commands;